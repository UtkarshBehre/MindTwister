<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mindtwister.mindtwister">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/login_activity" />
        <activity
            android:name=".RegistrationActivity"
            android:label="@string/registration_activity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AboutActivity"
            android:label="@string/about_activity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_activity" />
        <activity
            android:name=".InstructionsActivity"
            android:label="@string/instructions_activity" />
        <activity
            android:name=".PlayMenuActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RainbowMatrixActivity"
            android:label="Rainbow Matrix" />
        <!-- default code -->
        <!-- android:allowBackup="true" -->
        <!-- android:icon="@mipmap/ic_launcher" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:supportsRtl="true" -->
        <!-- android:theme="@style/AppTheme"> -->
        <!-- <activity android:name=".MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".activity_memory_matrixs_33"
            android:label="@string/memory_matrix" />
        <activity
            android:name=".activity_memory_matrixs_34"
            android:label="@string/memory_matrix" />
        <activity
            android:name=".activity_memory_matrixs_44"
            android:label="@string/memory_matrix" />
        <activity
            android:name=".activity_memory_matrixs_45"
            android:label="@string/memory_matrix" />
        <activity
            android:name=".activity_memory_matrixs_55"
            android:label="@string/memory_matrix" />
        <activity
            android:name=".activity_memory_matrixs_56"
            android:label="@string/memory_matrix" />
        <activity
            android:name=".activity_memory_matrixs_66"
            android:label="@string/memory_matrix" />
    </application>

</manifest>